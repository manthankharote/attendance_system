<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Select Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" crossorigin="anonymous">
    <style>
        :root {
            --primary-color: #4f46e5;
            --secondary-color: #10b981;
            --light-bg: #f9fafb;
            --dark-bg: #18181b;
            --card-bg: #ffffff;
            --card-bg-dark: #23232a;
            --text-color: #111827;
            --text-color-dark: #f3f4f6;
            --border-radius: 12px;
            --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(120deg, #e0e7ff 0%, #f9fafb 100%);
            color: var(--text-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 2rem;
            transition: background 0.3s;
        }
        .dark-mode {
            background: linear-gradient(120deg, #18181b 0%, #23232a 100%);
            color: var(--text-color-dark);
        }
        .container {
            max-width: 1000px;
            width: 100%;
            text-align: center;
        }
        .brand-header {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 2rem;
        }
        .brand-header img {
            width: 48px;
            height: 48px;
            border-radius: 8px;
        }
        h1 {
            margin-bottom: 0.25rem;
            font-size: 2.25rem;
            color: var(--primary-color);
            letter-spacing: 1px;
        }
        p.subtitle {
            color: #6b7280;
            margin-bottom: 2rem;
        }
        .login-options {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }
        .option-card {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            text-decoration: none;
            color: var(--text-color);
            transition: transform 0.3s, box-shadow 0.3s, background 0.3s, color 0.3s;
            position: relative;
        }
        .dark-mode .option-card {
            background-color: var(--card-bg-dark);
            color: var(--text-color-dark);
        }
        .option-card:hover {
            transform: translateY(-8px) scale(1.03);
            box-shadow: 0 12px 30px rgba(79,70,229,0.16);
            background: rgba(79,70,229,0.03);
        }
        .option-card h2 {
            margin-top: 1rem;
            margin-bottom: 0.5rem;
            font-size: 1.5rem;
        }
        .option-card p {
            color: #6b7280;
            font-size: 0.98rem;
        }
        .dark-mode .option-card p {
            color: #a1a1aa;
        }
        .option-card i {
            font-size: 2.7rem;
            color: var(--primary-color);
        }
        .feature-list {
            margin-top: 2rem;
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            color: #6b7280;
            font-size: 1rem;
        }
        .feature-list i {
            margin-right: 0.4rem;
            color: var(--secondary-color);
        }
        .register-prompt {
            margin-top: 2rem;
            font-size: 1.07rem;
        }
        .register-prompt a {
            color: var(--primary-color);
            text-decoration: underline;
            font-weight: 600;
            margin-left: 0.3rem;
        }
        .toggle-darkmode {
            position: absolute;
            top: 1.2rem;
            right: 2rem;
            padding: 0.5rem 1rem;
            background: var(--primary-color);
            color: #fff;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
        }
        @media (max-width: 600px) {
            h1 { font-size: 1.35rem; }
            .option-card h2 { font-size: 1.10rem; }
            .option-card i { font-size: 2rem; }
        }
    </style>
</head>
<body>
    
    <div class="container">
        <div class="brand-header">
            <!-- Replace src with your logo path -->
            <h1>Attendance System Portal</h1>
        </div>
        <p class="subtitle">Please select your role to login.</p>
        <div class="login-options">
            <a href="/auth/admin-login" class="option-card" aria-label="Admin login">
                <i class="fas fa-user-shield"></i>
                <h2>Admin</h2>
                <p>Login for administrative access.</p>
            </a>
            <a href="/auth/teacher-login" class="option-card" aria-label="Teacher login">
                <i class="fas fa-chalkboard-teacher"></i>
                <h2>Teacher</h2>
                <p>Access your classes and take attendance.</p>
            </a>
            <a href="/auth/student-login" class="option-card" aria-label="Student login">
                <i class="fas fa-user-graduate"></i>
                <h2>Student</h2>
                <p>View your attendance and QR code.</p>
            </a>
        </div>
        
        <div class="register-prompt">
            <p>New Student? <a href="/auth/register">Register here</a></p>
        </div>
    </div>
</body>
</html>
